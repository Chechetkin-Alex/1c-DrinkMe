# Бизнес-ценность

## Идея

DrinkMe -- сервис для заказа кофе, выпечки и товаров для кофе онлайн заранее. 

Мотивация: сделать локальный аналог Drinkit, но для жителей Долгопы. Сейчас, насколько я знаю, их там нет, а сценарий довольно интуитивный: студент/житель долгопы заранее выбирает кофе, булочку или пачку зерна, оплачивает, а потом просто забирает его без очереди. То есть модель Заказал, оплатил, дошел до магазина, забрал.

## Проблема

В районе МФТИ есть спрос на быстрые заказы еды и кофе на вынос (верим в предпосылку).

Обычный сайт кофейни часто просто показывает меню. В этом проекте я хочу сделать именно сервис, где пользователь может собрать заказ и оформить его заранее.

Киллер фичей станет студенческое комбо, субсидированное добрым дядей инвестором, выпускником физтеха. Оно будет доступно, если клиент в личном кабинете введет почту в формате "@phystech.edu".

## Целевая аудитория

Основная аудитория:

- студенты, сотрудники МФТИ
- жители долгопы

Также есть отдельная роль администратора кофейни. Он управляет товарами, категориями, остатками и статусами заказов.

## Основные сценарии

Пользователь может:

- посмотреть каталог напитков, выпечки и товаров
- открыть карточку товара
- добавить товары в корзину
- оформить заказ на вынос
- посмотреть историю своих заказов
- повторить похожий заказ в будущем

Администратор может:

- добавлять и редактировать товары
- менять остатки
- смотреть заказы
- менять статус заказа

## Первая версия

В первую версию входят:

- каталог напитков, выпечки, зерна и аксессуаров
- категории товаров
- карточка товара
- корзина
- оформление заказа
- регистрация и вход
- история заказов пользователя
- статусы заказа
- регистрация в домене @phystech.edu => студенческое комбо с меньшей ценой
- админка для управления товарами и заказами

## Планируемый стек

- Бэкенд: Python, Django, Django REST Framework
- Авторизация: токены Django REST Framework
- PostgreSQL для финального запуска
- Фронтенд: React, Vite, React Router
- Управление состоянием: Context API или Zustand
- Тесты: pytest, pytest-django
- Инструменты: gunicorn, ruff/black по возможности

## Основные модули

- `users` - пользователи, регистрация, профиль, права доступа
- `catalog` - категории, напитки, товары, остатки, цены
- `cart` - корзина пользователя
- `orders` - оформление заказов, позиции заказа, статусы
- `reviews` - отзывы к товарам

Документация в папке docs

- [Требования](docs/requirements.md)
- [Архитектура](docs/architecture.md)
- [База данных](docs/database.md)
- [REST API](docs/api.md)
- [Сценарий демонстрации](docs/demo.md)
- [Подготовка к защите](docs/defense.md)
- [Деплой](docs/deploy.md)

## Локальный запуск

Проект запускается двумя процессами:

- Django API на `http://127.0.0.1:8000`
- React-фронтенд на `http://127.0.0.1:5173`

Если случайно было запущено несколько серверов и появился белый экран, можно остановить локальные процессы:

```bash
pkill -f "vite"
pkill -f "manage.py runserver"
```

После этого лучше запустить все заново по шагам ниже.

## Локальный запуск бэкенда

Создать окружение:

```bash
python3 -m venv .venv
```

Установить зависимости:

```bash
.venv/bin/python -m pip install -r backend/requirements.txt
```

Проверить проект:

```bash
cd backend
../.venv/bin/python manage.py check
```

Применить миграции:

```bash
cd backend
../.venv/bin/python manage.py migrate
```

Наполнить базу демо-данными:

```bash
cd backend
../.venv/bin/python manage.py seed_demo
```

Запустить сервер:

```bash
cd backend
../.venv/bin/python manage.py runserver 127.0.0.1:8000
```

После этого в админку можно войти с логином `admin` и паролем `222333`.

## Локальный запуск фронтенда

Установить зависимости:

```bash
cd frontend
npm install
```

Запустить фронтенд:

```bash
cd frontend
npm run dev -- --host 127.0.0.1
```

Фронтенд будет доступен по адресу:

```text
http://127.0.0.1:5173/
```

Для работы фронтенда должен быть запущен бэкенд на `http://127.0.0.1:8000/`.

Останавливать бэкенд и фронтенд можно через `Ctrl + C` в каждом терминале.

## Что проверить руками

После запуска можно проверить основные сценарии:

- открыть каталог
- найти `капу` через поиск
- зарегистрироваться на обычную почту
- зарегистрироваться на почту `@phystech.edu`
- добавить кофе в корзину
- выбрать объем кофе в карточке каталога
- оформить заказ
- открыть страницу заказов
- войти под `admin` и поменять статус заказа
- купить товар и после этого оставить отзыв

Студенческое комбо доступно только пользователям с почтой `@phystech.edu`.

Пользователь с обычной почтой может оформлять обычные товары, но не может добавить студенческое комбо.

## База данных

Локально проект по умолчанию работает на SQLite:

```text
backend/db.sqlite3
```

Для хостинга добавлена поддержка PostgreSQL через переменную окружения `DATABASE_URL`.

Если `DATABASE_URL` задан, Django использует PostgreSQL.

Если `DATABASE_URL` не задан, Django использует локальную SQLite-базу.

## Быстрая проверка

Проверить бэкенд:

```bash
cd backend
../.venv/bin/python -m pytest
```

Проверить сборку фронтенда:

```bash
cd frontend
npm run build
```

## Минимальный прод-запуск бэкенда

Для простого хостинга бэкенда используются переменные окружения:

- `DJANGO_SECRET_KEY`
- `DJANGO_DEBUG`
- `DJANGO_ALLOWED_HOSTS`
- `DJANGO_CORS_ALLOWED_ORIGINS`
- `DJANGO_CSRF_TRUSTED_ORIGINS`
- `DATABASE_URL`
- `DJANGO_SECURE_SSL_REDIRECT`
- `DJANGO_SESSION_COOKIE_SECURE`
- `DJANGO_CSRF_COOKIE_SECURE`
- `DJANGO_SECURE_HSTS_SECONDS`
- `DJANGO_SECURE_HSTS_INCLUDE_SUBDOMAINS`
- `DJANGO_SECURE_HSTS_PRELOAD`

Пример:

```bash
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=example.com
DJANGO_CORS_ALLOWED_ORIGINS=https://frontend.example.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://backend.example.com,https://frontend.example.com
DATABASE_URL=postgresql://user:password@host:5432/dbname
```

Команда запуска для платформ, которые читают `Procfile`:

```text
web: python manage.py migrate && python manage.py seed_demo && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
```

Для Render добавлен файл `render.yaml`. Он описывает:

- PostgreSQL-базу
- Django web service
- React static site

Фронтенду на хостинге нужна переменная:

```text
VITE_API_BASE_URL=https://your-backend.onrender.com/api
```

Подробный порядок деплоя описан в [docs/deploy.md](docs/deploy.md).
